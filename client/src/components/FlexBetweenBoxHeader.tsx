import {Box} from "@mui/material";
import {styled } from "@mui/system";

const FlexBetweenBoxHeader = styled(Box)({
    display:"flex",
    justifyContent:"space-between",
    alignItems: "center"

});

export default FlexBetweenBoxHeader;